// 사용자 Main
import styled from 'styled-components';

const Wrapper = styled.div`
  background-color: skyblue;
`;

const Main = () => {
  return <Wrapper style={{ width: '100%', height: '100%' }}></Wrapper>;
};

export default Main;
